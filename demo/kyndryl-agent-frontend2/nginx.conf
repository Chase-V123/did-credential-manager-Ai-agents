server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Enable gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Proxy NANDA API requests to avoid CORS
    location /api/nanda/ {
        proxy_pass https://nest.projectnanda.org/api/;
        proxy_set_header Host nest.projectnanda.org;
        proxy_set_header Origin "";
        proxy_ssl_server_name on;
    }

    # Proxy internal registry API
    location /api/registry/ {
        proxy_pass http://did-registry:5004/;
    }

    # Proxy orchestrator API
    location /api/orchestrator/ {
        proxy_pass http://did-orchestrator:5005/;
        proxy_read_timeout 60s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
